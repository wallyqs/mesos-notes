#+title: Notes on mesos

- Simple command scheduler basing on the example scheduler from =mesos-go=
- Example version of a =DockerCommandScheduler= which runs the command inside Docker
- Experimental scheduler for Org Babel documents =OrgBabelScheduler=
